--- 

# This is an example of the vars.yml file. Before executing the playbook, please 
# make any necessary changes to this file to fit your use case, then rename the 
# file to vars.yml. 

system_type: "laptop"  # One of "laptop", "desktop", "nas"
primary_user: "aj"  # The main user account. Change this to your username
preferred_shell: "zsh" 
hostname: "{{ system_type }}"
dnf_max_parallel_downloads: "15"
dnf_fastestmirror: "True"
